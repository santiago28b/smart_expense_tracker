# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project Overview

This is a smart expense tracker project with in Python since this is a tutorial there are a lot of steps that the user does not fully understand. evreytime the user makes a prompt after sending the response ask the user whether they understand everything claude sent or did and if the user would like a quiz to test understanding. always provide feedbacl to keep best system desing and python practices. the user wants to develop skills that will make him hireable for software engineer role in the future

## Environment Setup

```bash
source venv/bin/activate   # activate the virtual environment
pip install -r requirements.txt  # once requirements file exists
```

## Architecture

- `backend/` — Python-based
